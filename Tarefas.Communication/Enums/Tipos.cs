namespace Tarefas.Communication.Enums;
public enum PrioridadeType {
    Baixa = 0,
    Média = 1,
    Alta = 2
}

public enum StatusType {
    Aguardando = 0,
    Concluída = 1,
    EmAndamento = 2
}
